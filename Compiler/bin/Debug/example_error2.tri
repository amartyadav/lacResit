!this is an example program
let
	const MAX ~ 10;
	var n: Integer
in
	{
		getint(var n);
		if n > 0 then
			if n < MAX then
				while n > 0 do
					{
						putint(n); puteol();
						n := n-1
					}
			else
		else
	}
